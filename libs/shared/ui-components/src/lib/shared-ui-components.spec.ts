import { sharedUiComponents } from './shared-ui-components';

describe('sharedUiComponents', () => {
  it('should work', () => {
    expect(sharedUiComponents()).toEqual('shared-ui-components');
  });
});
